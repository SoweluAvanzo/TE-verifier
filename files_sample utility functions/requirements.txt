# TOKEN ECONOMY SIMULATOR — REQUIREMENTS
# ========================================
# Install with: pip install -r requirements.txt
#
# Tested with Python 3.12
# cadCAD 0.5.3 uses Experiment() API —
# see run_simulation() in token_economy_simulation.py

cadCAD==0.5.3
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
